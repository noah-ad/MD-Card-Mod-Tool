# 2.0.15 — 手机资源接入统一工作区（本地测试版）

- “选择目录”直接接受手机端已解压的 0000 或其父目录；进入现有主窗口，不再弹出独立手机浏览／修改器。上次目录可恢复，手机目录与 PC 账号的索引隔离。
- 同一套卡名搜索、卡图预览／裁剪／替换、卡套／灵摆显示映射、透明／炫彩超框编辑、视觉资源、动画页、Mod 导入导出和备份还原。手机资源使用实际 Bundle 扫描，不套用 PC 文件大小筛选和预绑定资产位置。
- 识别手机端只有一套完整档位的官方动画。22524 手机数据只有 Texture、Atlas、Skeleton 三个 Bundle，不再因缺少另一套 PC HD/SD 资源被判为不支持。按实际容器和档位配对，不复制不存在的资源。
- 手机动画生成和其他卡动画移植沿用原事务／回滚流程，但输出 RGBA32 而非 PC BC7。文件和内存占用可能增加；没有官方动画的卡仍不支持创建召唤触发。
- 手机直接替换 ZIP 使用 `0000/<两位目录>/<八位文件名>`。带新版清单的 Mod 记录平台，拒绝已知 PC／手机跨平台误装；旧版和无清单包仍需用户自行确认来源兼容性。
- 备份、超框编辑草稿使用原工具布局，位于当前 0000 下的 `_MD卡图备份` 等工具目录。传回手机时仅复制实际哈希资源，或使用直接替换 ZIP，不复制工具元数据。2.0.14 独立修改器的 `_MDMobileMods` 备份不会自动迁移。
- 已验证手机实际文件的临时镜像：卡图／视觉纹理替换、Mod 标记、ZIP 往返、超框登记与还原、22524 动画生成／预览／失败回滚／还原，以及 3413 动画移植到 22524。主窗口加载手机索引并预览通过。尚未完成全目录扫描与手机游戏内验收，不声称所有资源均已验证。
