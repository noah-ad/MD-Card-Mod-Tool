# 2.0.10 — 预览滚动与 Mod 导入卡片身份修复

- 卡片／视觉资源页右侧预览保留至少 320 逻辑像素的区域，不再被信息和按钮挤成细条。
- 右侧内容不足以完整显示时启用深色垂直滚动条，支持鼠标滚轮、拖动滚动条、Tab 聚焦按钮时自动滚动到可见位置。
- 放大窗口后恢复无滚动布局；反复缩小、放大不累积空白高度。图片继续等比预览，不拉伸。
- 保留 2.0.9 的动画比例校准：新版 100% 等于 2.0.8 的 140%。本轮没有再次调整动画生成比例。
- 导入 Mod 后保留全部资源显示，不再自动启用“仅显示 Mod”而隐藏原有卡片。
- 卡图重新扫描按目标 Bundle 的逻辑路径 CRC 识别卡号，不再被外部 Mod 的来源贴图名称误导；导入后刷新重打包改变的 PathID 和 serialized file 定位。已错认的旧索引可重建恢复。
- 在临时镜像中把另一张卡的 Bundle 以 22524 的目标路径导入，验证卡号、预览、数字搜索、首次备份和还原；真实游戏文件哈希未变。

验证：初始 DPI 注入与不同高度视口下检查最小预览高度、滚轮、底部按钮可达性、放大后滚动条复位；未修改游戏文件。实际桌面 DPI／跨屏效果仍需用户复测。
