# 2.0.16 PC Mod 索引修复测试版

- 修复电脑端索引混入其他资源目录后，Mod 管理有数量但没有卡片的问题。
- 载入时验证纹理路径归属，污染索引保留为 `.foreign-*.bak`，随后重新建立本机索引，并重读有备份的卡图及视觉资源。
- 拒绝保存混有其他目录或账号资源的索引，缓存写入改为临时文件后原子替换。
- 扫描、Mod 状态和超框分类增加工作区快照检查，阻止过期异步结果覆盖当前页面。
- 手机端兼容开发与测试暂停；已有代码保留，本版不承诺手机端可用。

使用独立目录中的 EXE 启动并选择原电脑游戏目录/账号。首次读取旧索引会自动修复；无需重新导入已安装的 Mod。请保留游戏根目录 `_MD卡图备份`。

验证：污染索引副本恢复；15 个卡图 Bundle、1 个视觉 Bundle 全部重新获得 Mod 标记（27 张纹理）；MainForm 的 Mod 过滤列表显示 15 个卡图条目。96 个游戏/备份文件 SHA-256 前后相同。PC Mod 包导入/导出、账号映射、备份、回滚和 10 项拒绝测试通过。未进行真实游戏写回测试。

源码由本机残存 2.0.15 程序恢复，恢复局限见 RECOVERY-REPORT.md。本次为独立测试版，不覆盖原程序，也未上传 GitHub。
