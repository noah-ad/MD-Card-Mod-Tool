# MD Card Mod Tool 2.0.1

发布日期：2026-08-31

## 修复

- 统一制作超框编辑器、导出的游戏预览与卡片资源主预览。三处现在都使用同一套 Master Duel RGB 投影，不再出现编辑器中完整、应用后主界面只剩部分图层的错觉。
- 新增“游戏最终预览／构图编辑”双模式。窗口默认显示最终结果；拖动、滚轮、滑杆与方向键编辑只在构图模式生效，避免把最终预览误当成可编辑画布。
- Texture2D 解码与 PNG 编码显式保留 Alpha=0 像素中的 RGB；这些像素只在预览副本中设为可见，写回数据的 Alpha 不会被改成不透明。
- 修复透明 RGB 在 decode → PNG → Bundle → decode 往返中可能受图片编码器默认值影响的问题。

## 发布目录

- 发布包根目录只保留 `MD卡图查看替换器.exe` 和 `data` 文件夹。
- 卡名目录、索引、卡框、超框模板、工具、说明、启动兼容脚本与许可文件全部放入 `data`；程序仍兼容旧目录作为只读回退。

## 验证

- 真实 22747 草稿的 16 套 Astellar 模板、48 套分类卡框与统一编辑器回归全部通过。
- 在临时复制的 22747 Bundle 中写入并重新解码，704×1024 全部 RGBA 像素差异为 0；139,993 个透明 RGB 像素全部保留。
- 测试前后真实游戏 Bundle 的 SHA-256 一致；未向游戏文件写入测试数据。

完整功能与安全边界见 `README.md` 和 `TEST-REPORT.md`。
