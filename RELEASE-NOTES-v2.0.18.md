# 2.0.18 超框应用等待修复

修复正在合成时点击应用，会让当前预览失效、安排下一轮合成却立即检查空结果的问题。现在应用会等待运行中的合成，以及待更新的最新构图；生成失败时显示具体原因。应用期间禁止继续编辑或重复提交，写入前固定本次图片数据。

保留 2.0.17 单卡“去除闪卡／镜碎内框”实验选项。该选项游戏内效果仍待用户验证，不能将编辑器测试视为实际闪面效果验证。

独立测试版，不覆盖旧版、不自动写入游戏、不上传 GitHub。手机兼容继续搁置。
