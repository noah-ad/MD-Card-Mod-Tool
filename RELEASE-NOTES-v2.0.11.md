# 2.0.11 — 支持 8192 动画图集

- 怪兽动画的单张图集上限增加 8192 选项，默认仍为 4096；最终尺寸由素材和打包结果决定，不会强制填满 8192。
- 同步放宽 HD／SD 写入校验至 8192，避免大图集写回时被旧的 4096 校验拒绝。格式、引用及回滚检查继续保留。
- 8192×8192 的单张 RGBA 原始数据约 256 MiB，生成、压缩和预览的峰值内存更高，耗时也可能增加。内存不足时请选择 4096 或降低帧数／分辨率。
- 图集容量不改变动画在游戏里的显示比例；原有 100% 校准保持不变。尚未验证真实游戏内 8192 播放兼容性。

验证：最终 EXE 已通过真正的 8192×8192 HD 图集生成、临时 22524 六 Bundle 写回、尺寸回读、Spine 渲染、故障回滚、首次备份和还原。两帧测试正常提交约 23 秒，非长视频性能测量；真实游戏文件哈希未变。
