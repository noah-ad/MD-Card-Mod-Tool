# 2.0.19 自定义卡框去内框修复

修复勾选去内框时，自定义卡框标识 `__custom__` 被误用为 Astellar 模板名，导致无法生成及应用的问题。自定义 PNG 保持原样，仅闪面遮罩按卡片目录中的卡类型选择；缺少卡类型数据时沿用效果怪兽默认模板。

新增自定义 PNG + 去内框 + 应用准备回归测试。真实游戏内去框效果仍需实测；未自动改写游戏文件。关闭去内框并重新应用可恢复默认遮罩。
