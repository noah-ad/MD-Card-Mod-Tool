# 2.0.19 自定义卡框去内框修复

修复勾选去内框时，自定义卡框标识 `__custom__` 被误用为 Astellar 模板名，导致无法生成及应用的问题。自定义 PNG 保持原样，仅闪面遮罩按卡片目录中的卡类型选择；缺少卡类型数据时沿用效果怪兽默认模板。

新增自定义 PNG + 去内框 + 应用准备回归测试。真实游戏内去框效果仍需实测；未自动改写游戏文件。关闭去内框并重新应用可恢复默认遮罩。

## 本次公开发行包含

- 2.0.16：电脑端污染索引备份、自动修复及 Mod 列表显示修复，阻止跨工作区索引覆盖。
- 2.0.17：单卡“去除闪卡／镜碎内框”实验选项，默认关闭，不修改公共卡框，可能影响局部闪度。
- 2.0.18：应用前等待最新合成完成，生成失败显示具体原因，防止重复提交。
- 手机兼容暂停开发与测试，已有代码保留但不承诺可用。
- 源码基于残存 2.0.15 EXE 恢复，详见 RECOVERY-REPORT.md。

## 验证与安装

15 套模板像素测试、四种卡框编辑流程、生成中应用、自定义 PNG 去内框及应用准备测试通过。未验收真实游戏闪面效果。请退出游戏后应用，并保留原始备份。

解压 ZIP 到独立目录，运行根目录 EXE；data 文件夹必须保留。无需覆盖旧版本，无需重新导入已安装 Mod。
