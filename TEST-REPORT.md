# MD Card Mod Tool 2.0.10 测试报告

## 2.0.10（2026-09-06）

- 初始 96／144／192 DPI 注入，320／480／1100／360 高度视口反复调整：预览最小高度、等比模式、滚轮、底部可达性、大视口滚动复位通过。144 DPI／480 高度截图目检合成圆形未被压扁。真实桌面为 96 DPI，未验收真实 150% 或跨屏。
- 22524 临时 LocalData 镜像：将 3801 原始 Bundle 以 22524 目标路径导入，覆盖外部重打包不同名称／PathID 场景。导入、重新扫描卡号、资产定位刷新、预览解码、数字搜索、首次备份与还原通过。真实 22524／3801 文件哈希不变。
- UI/UX 技能用于保留内容尺寸、以溢出滚动取代压缩预览；不改变动画生成比例。
- 最终自包含 EXE 四项回归退出码均为 0：预览滚动、22524 导入身份、主窗口四语／导航布局（clipped=False）、Mod 包兼容与安全校验（旧包／直接 ZIP／无清单 ZIP、四种分类、账号映射、备份、回滚和 10 项拒绝用例）。游戏写回仅临时镜像。

---

## 2.0.9（2026-09-06）

- 用户指定校准：新 100% 输出 6720×3780，等价旧 140%。22524 临时六 Bundle 实际替换后，单槽预览回读为 100%；HD/SD 几何、渲染四角覆盖、提交故障回滚、首次备份与还原均通过。占用拒绝 1128ms、两帧 160×90 正常提交 1396ms；不是大视频性能测量。
- 回读回归发现旧解析器不支持无缩进的 bounds/offsets Atlas；已补充支持并使用同地区同档位资源配对。首次几何断言因浮点乘法精度失败，已改为 0.001 容差；失败日志仍保留。
- 96／144／192 初始 DPI 注入及 100／150／200% 布局模拟通过。真实桌面仍为 96 DPI，实际 150% 和跨屏未验收。
- Release 自包含发布完成。另一个旧 CLI build-profile 经单图片媒体提取未结束，已终止该测试进程，不计作通过；比例验证由直接两帧构建与 Bundle 往返覆盖。
- 未修改或注入真实游戏；140% 为手动校准，不保证所有卡、分辨率全屏或无裁边。新版须重新替换，旧 Mod 不自动重写。

---

# 2.0.8 历史测试报告

## 2.0.8（2026-09-06）

- 最终 EXE 卡名测试：真实游戏只读提取 14,323 条记录，22920 为「小丑戏帮 哈特」，卡号及「哈特」搜索通过。原用户缓存确实没有 22920；未直接修改用户缓存。
- 临时镜像中的同 Build 字典更新：缓存缺名、原路径文件更新时间改变后，UpdateIfNeeded 自动补齐中文并保留已有英文测试翻译。资源未变命中缓存、旧格式失效、资源指纹变化触发刷新均通过。
- 初版全量扫描方案过慢，已中断其只读测试，未把它作为通过结果。最终实现从真实容器识别数据版本目录，验证 CRC 映射后定位同版本各语言字典；未知结构保留扫描回退。最终日志在扫描 1,000/40,507 后定位当前字典并完成，不再无条件扫描所有纹理。
- 最终 EXE 的 22524 临时六 Bundle：占用预检 1,122ms，正常提交 1,560ms（仅两帧 160×90 合成素材，不代表大视频性能）；提交中断回滚、首次备份与还原通过。HD/SD JSON 无隐藏缩放和漂移；重新读取写出 Bundle 后，所选 HD 预览的 24 个采样帧四角覆盖检查通过。真实游戏文件哈希未变。
- 最终 EXE DPI 回归：额外注入初始 96／144／192 DPI，直接覆盖构造阶段的 None → Dpi 模式切换。侧栏 222／334／444px、按钮 44／66／88px、文字留白 43／64／86px；另有打开后 100／150／200% 布局模拟，均通过。
- 最终 EXE 主窗口回归：四语、四导航、资源页与动画页切换、搜索栏位置均通过，clipped=False；首切 13ms、返回 34ms（本次测量）。100% 侧栏图片目检图文未重叠。
- Release 与 recovered-src 镜像构建均 0 错误，101 个既有 nullable 警告。最终包根目录仅 EXE 与 data，FFmpeg／texconv 均存在。EXE 版本 2.0.8.0，78,256,170 字节，SHA-256 `D883A96118BEB47CAD9D05BCFA3924954A42753908B0F50EEDC19F6385935DC0`。
- 限制：实际桌面 DPI 仍为 96，注入／布局模拟不等于真实 150% 或跨屏验收；未启动、修改或注入游戏进程，未进行实际召唤验收。用户需要用新版重新生成旧视频 Mod。非 16:9 素材保持比例，不保证无留边。未上传 GitHub。
- UI/UX 技能用于一致缩放基准、侧栏图文间距与标题分行；没有扩大到无关界面重设计。

---

# 2.0.7 历史测试报告

## 2.0.7（2026-09-06）

- 22524 真实六资源的临时镜像：最终 EXE 模拟 FileShare.Read 占用，1112ms 提前拒绝，未进入编码；正常构建／提交、第二项提交后注入故障回滚、首次备份还原均通过。两帧 128×128 合成测试的提交流程 1407ms，不代表用户大视频耗时。
- 最终本地测试 EXE：版本 2.0.7.0，78,250,700 字节，SHA-256 `9A09E90F47FB04315D60896FCEC6FCD522A0165AC62A33B3CA9779409885854C`。尚未上传 GitHub，未进行真实游戏召唤验收。
- 其他卡整套动画移植：六文件验证、失败回滚、还原通过。真实游戏 Bundle 的 SHA-256 未改变。
- 100%／150%／200% WinForms 布局缩放回归：侧栏 222／334／444px，按钮 44／66／88px，左文字留白 43／64／86px，均通过。测试机实际 DPI=96，未改变用户系统设置；这不等于真实 150% 桌面或跨屏 WM_DPICHANGED 验收。
- 已移除错误标题中无条件的“已回滚”；正式文件只在全套临时资源校验成功后提交，回滚失败保留恢复副本。

---

# 2.0.6 历史测试报告

## 2.0.6 发布验证（2026-09-05）

- Mod ZIP：旧版工具包、保留游戏目录的直接替换 ZIP、无清单 ZIP 均往返通过。覆盖四种资源分类、当前账号映射、首次备份不被覆盖及第 2 项提交后故障回滚；真实游戏文件哈希未变。
- 目录兼容：LocalData、StreamingAssets、0000、两位哈希子目录及单层分享包前缀通过。10 项异常用例拒绝：越界、绝对路径、ADS、重复路径、符号链接、多个账号映射冲突、伪装程序文件、目标不存在、损坏 Bundle、清单校验不符。所有异常导入未改变目标。
- 22524：高清 0.585 和标清 0.2925 六资源正确配对。模拟相同 Build 的旧缓存缺少标清资源，EnsureCurrentIndex 自动补全；重复更新资源数不增加。资源均来自临时镜像，gameWrites=False。
- 最终包生成 22524 首／中／末帧：39 帧、24 fps；这验证资源关联及可渲染，不代表所有复杂骨骼约束与游戏逐帧完全相同。
- 主窗口布局通过：标题 2.0.6、四语、四导航、PerMonitorV2、无裁切。超框四模式切换、快速切换、旧灵摆草稿迁移通过，透明像素 129,418，预览／输出像素差异 0。
- Release 编译 0 错误，101 个既有 nullable 警告。发布物已包含 FFmpeg、texconv、卡片与动画目录、卡框和许可；源码不捆绑 FFmpeg，编译时该打包提醒属预期。
- 输出 MD-Card-Mod-Tool-v2.0.6-test，根目录仅 EXE 与 data；最终 EXE 版本 2.0.6.0、78,246,060 字节、SHA-256 `771196FBE5EB6BEEFD5980CC55DBF69D95716C162D0559F2946BB6AA600A87D4`。
- 未修改或注入实际游戏进程，未进行真实游戏召唤验收。以下 2.0.5／2.0.4 是历史回归记录，上传状态与旧哈希只属于相应版本。

---

# 2.0.5 历史测试报告

测试日期：2026-09-05。以下为本轮新增回归；后附 2.0.4 历史记录，不代表旧结论已全量重新验证。

## 2.0.5 验证结果

- Debug／Release 构建：0 错误，101 个既有 nullable 警告。源码不捆绑 FFmpeg；发布包已核对包含 ffmpeg.exe 与 texconv.exe。
- `--test-studio-optimizations`：0°／90°／180°／270° 图集坐标、旋转区域几何、mesh 裁边偏移、父子骨骼关节变换通过；背景与卡图独立操作，960×720 原图保留，草稿往返输出一致。窗口缩放不会改变两层的构图参数。
- `--test-animation-transfer`：真实 3413 动画移植到 10001 的临时 Bundle 镜像，6 个文件全部重新打开可用；在第 2 项提交后注入异常，回滚成功；正常替换后的备份还原成功。目标和来源的真实游戏文件 SHA-256 均未改变，`gameWrites=False`。
- `--test-animation-donor-picker`：四种界面语言下输入“青眼”均定位 3801，来源预览加载成功；窗口关闭释放预览帧。新增按钮、列表和独立播放区已进行构造与交互回归。
- `--test-overframe-workflow`：统一编辑器、四类各 16 套卡框、真实 Alpha、原有卡图拖动、草稿恢复、旧灵摆草稿迁移通过。
- 3401、3413、3801、10001 的首帧／中间帧／末帧已生成到 `test-artifacts/v2.0.5/after<卡号>`；3413 修复前后样本位于 `before3413`／`after3413`。已目检的 3413 与 3801 样本未见此前的旋转贴图错位。这是抽样验证，不是所有 Spine 特性逐帧等价的证明。
- 最终 EXE 连续操作屏幕回归：动画页 `repaintDifference=30`，超框页 `repaintDifference=31`，原阈值 64 不变，`ready=True`。覆盖播放／暂停、全部圆角按钮 hover、页面往返、窗口尺寸变化及四类卡框切换，证据位于 `package-ui-verified`。
- 修复收尾时发现的两项回归：超框工具栏容器留白造成按钮底部裁切；仅调整窗口大小也会使逻辑构图缓存失效。现在保留未改变的构图输出，只有真实构图变化才重新合成。之前未通过的 `package-ui-final` 原始截图仍保留，没有删除失败证据。
- CLI 的 DoEvents 消息循环改为显式持有 UI 同步上下文，并开启非法跨线程访问检查。连续快速切换四类卡框通过；最终透明输出有 129,418 个透明像素，预览与导出逐像素差异为 0。
- 最初两次屏幕回归被外部 ChatGPT／Codex 任务栏缩略图覆盖底部；差异位于客户区 y=822 以下。最终测试仍使用真实屏幕像素，最终捕获窗口缩小至避开遮挡的高度，未屏蔽差异像素或调高阈值。各次原始截图及结果均保留在 `ui`、`ui-recheck`、`ui-final`。

## 2.0.5 范围与限制

- 独立输出：`MD-Card-Mod-Tool-v2.0.5-test`，根目录仅有 `MD卡图查看替换器.exe` 和 `data`。未覆盖 2.0.4，未上传本轮修改到 GitHub。
- 最终 EXE：文件版本 `2.0.5.0`，78,235,648 字节，SHA-256 `CF88DD57B9B610982A8943F10D51FEAA42C5E8CCA52B75D91C2028222F46408F`。
- 启动检查：InputIdle 与 Responding 通过；原生窗口检查标题为“MD 卡片 Mod 工具 2.0.5”，正常关闭后进程退出。最初以 Hidden 方式启动的测试实例不提供可见主窗口，因此单独结束该测试实例后改用原生窗口验证；未关闭其他用户进程。
- 最终包的背景、透明超框、卡套／灵摆比例、主窗口布局均通过；`clipped=False`，四语、四导航、动画页隐藏卡名搜索通过。主窗口构造测试测得首切 15ms、返回 35ms，仅为本机该次测试结果，不代表所有设备性能。

- 游戏资源只读，写回测试仅针对测试目录中的副本；未启动或注入游戏，未进行真实游戏召唤验收。
- 来源动画替换保留目标资源名、容器及触发动画名称；不是“让没有原生动画的卡获得触发”。目标缺少完整 HD/SD 或 Spine 主次版本不兼容时拒绝写入。
- 本轮明确修复图集旋转、尺寸和裁边 UV。独立预览器中复杂 IK、path、transform、physics 的既有实现仍需逐资源验证，不能沿用旧版“非空帧扫描通过”推断全部动画正确。
- UI/UX 技能用于静态青金色几何、稳定 hover、DPI 图标与后台合成；未加入持续装饰动画，以减少闲置重绘。

---

## 2.0.4 历史测试记录

测试日期：2026-08-31
测试平台：Windows x64、.NET 8、自包含发布
本机游戏 build：`24462996`

## 构建与发布

- Release 构建：0 error、101 个既有 nullable 静态分析 warning；warning 不阻断发布。
- `win-x64` 自包含发布输出到独立的 `MD-Card-Mod-Tool-v2.0.4-test`，不覆盖 2.0.3 或更早版本；发布根目录只允许 EXE 与 `data` 两项。
- 最终 2.0.4 测试包重新执行卡套／灵摆双向映射、统一超框、64 套逻辑卡框、真实 Alpha 预览、卡名检索和真实多步骤 UI 交互验收，全部 `ready=True`；EXE 文件版本 `2.0.4.0`，大小 78,223,870 字节，SHA-256 为 `15C608A75CB9ED8EE25E22AD2BF95CE73ED6495534CA386D8AEB8F1FA051653B`。当前未配置 Authenticode 代码签名，状态为 `NotSigned`。
- 发布包资源核验通过：四语卡片目录、动画索引、16 套 Floowan 普通卡框、16 套 Astellar 透明卡框、16 套 Floowan OfGradient 炫彩框，以及由后二者运行时派生的 16 套透明炫彩框；16×6 共 96 个 Astellar 超框图层、`classdata.tpk`、ffmpeg、texconv、SkiaSharp 许可与两项目署名均存在。
- 发布包不包含 `test-artifacts`、已退役运行时桥接器、原生 `build`、C/C++ 源码、游戏 Bundle 或 Spine 兼容扫描报告。

## UI、本地化与游戏定位

- 主窗口 1180×760 布局回归：`dpi=PerMonitorV2`、4 个导航入口、4 种语言、无控件裁切，游戏与账号状态可用；不存在独立的卡框／超框或 Mod 管理一级导航。
- 主窗口实机视觉验收：Master Duel 深色侧栏、圆角纳西妲图标、页面标题层级、焦点边框和 4 个一级导航均正确显示；圆角按钮不再出现矩形黑边。
- 2.0.4 连续操作残影回归：3899 只读回退到 P13668 后执行预览、播放／暂停、所有圆角按钮 hover、窗口缩放、卡片页／动画页往返；超框编辑器依次切换透明、透明炫彩、炫彩、普通四类卡框，并切换卡框、缩放、真实 Alpha／构图模式、背景图及窗口尺寸。最终自包含发布包的动画页和超框页在交互截图与强制整窗重绘截图之间均为 0 像素差异（阈值 64），`animationReady=True`、`overFrameReady=True`、`ready=True`，未出现越界文字、导航残影或按钮旧帧。
- 圆角按钮回归确认每个控件使用独立 32-bit 离屏表面，`OptimizedDoubleBuffer=False`，并启用 sibling clipping；页面切换、主窗体布局稳定化和嵌入动画窗体均不再调用同步 `Update()` 或通过 `Hide / Show / Application.DoEvents` 重置画面。
- 侧栏导航调色板竞争回归：模拟旧 hover 动画仍运行时切换选中状态，220 ms 后 `navigationPaletteStable=True`；2.0 实机连续执行“卡片资源 → 视觉资源 → 卡片资源”，选中填充色与当前内容页始终一致。
- 视觉资源快捷筛选条改为 8 列自适应布局；在当前 Per-Monitor V2 系统缩放下无原生白色滚动条、无横向溢出，全部分类入口保持同一深色视觉体系。
- 卡片资源操作区 2×4 固定网格回归：8 个按钮全部位于预览下方且无 AutoScroll，“制作超框”可直接读取当前 Texture2D 原卡图。动画页操作区改为三行：选择素材、播放、替换／还原；圆角按钮保持矩形子窗口并仅自绘圆角，避免 DPI 重排后留下旧字符。
- 卡片资源选择“超框卡图”时显示超框上下文动作，选择分类树顶部“Mod 管理”时显示 Mod 上下文动作；四语卡名搜索仅在卡片资源页可见。超框登记表使用深色自绘表头与斑马行、自绘纵向滚动指示器，没有原生白色横向滚动条；2 个输入框和 4 个操作按钮使用统一圆角控件。
- 真实本机 40,054 Bundle 只读扫描在进度 100/40,054 且任务仍活跃时打开超框登记：调用耗时 0 ms，后台扫描收到取消并完整退出，单次消息处理最大 155 ms，管理窗口与自绘映射表正常出现。
- 发布前实机验收曾捕获“Build 扫描进行中点击卡框页”短暂触发 Windows“未响应”的竞争条件；修复后四语扫描与超框扫描均限制为 2–4 路，交互页会先暂停并等待旧扫描退出，且超框表定位、读取、备份检查由三次 Bundle 打开合并为一次。相同场景复测未再出现假死或幽灵窗口。
- 大列表事务与虚拟化回归：模拟 18,000 条资源时筛选、虚拟行建立及选中定位低于 100 ms；切换分类后稳定显示 9,000 / 18,000，未再出现“空分类树、0 / 0 与旧列表并存”的中间状态。
- 简中、繁中、日语、英语即时切换：主窗口、怪兽动画窗口和原始动画资源窗口均通过构造与切换测试。
- 四语卡名／卡号目录：14,203 张卡；“青眼白龙／青眼白龍／Blue-Eyes White Dragon／青眼の白龍”均定位到卡号 3801。
- 卡名包含匹配回归：输入“独角”返回 19 个候选，并包含“转生炎兽独角兔”（卡号 14338）；IME 组合完成后光标保持在输入末尾。
- 本机游戏数据增量合并后目录为 14,459 张；主窗口输入“小夜”返回 7 个带卡号、当前语言卡名及类型的候选。
- Steam 与 LocalData 定位：正确保留安装目录中的双空格；识别 build `24462996` 和本机 LocalData 账号。
- Release 布局回归通过：窗口标题与侧栏版本均为 2.0.4，动画页标题区、操作区、导航和独立按钮缓冲均为 `True`。最终自包含 EXE 启动冒烟为 `InputIdle=True`、`Handle=True`、`Responding=True`，窗口标题“MD 卡片 Mod 工具 2.0.4”；`CloseMainWindow` 正常关闭，`Forced=False`。

## Texture2D 与视觉资源

- 卡套 Texture2D 使用“正常编辑／预览／导出 `704×1024`，写入存储 `512×1024`”映射；灵摆卡图使用“正常编辑／预览／导出 `512×683`，写入存储 `512×1024`”映射。两者均拉伸完整画布，不再裁切旧版顶部 596 像素，也不在底部补透明尾部。
- 合成双向映射回归：`sleeve=704x1024->512x1024:True`、`pendulum=512x683->512x1024:True`、`fullCanvas=True`、`frameAspect=True`、`cropUi=True`、`ready=True`；尺寸同为 `512×1024` 的非卡套／非灵摆视觉资源保持 `Native`，不会误套映射。
- 旧版灵摆超框草稿迁移回归：即使目标 Texture2D 已经变成 `704×1024`，仍会根据草稿实际 `512×1024` 尺寸和卡片／卡框元数据迁移到 `512×683`，结果为 `legacyOverFrameDraft=PendulumCardArt:512x683`。
- 真实游戏 Bundle 临时副本回归：卡套 `ProtectorIcon1070002` 完成 `704×1024 -> 512×1024` 往返；灵摆卡 20406 完成 `512×683 -> 512×1024` 往返；两者均为 `geometry=True`、`original=True`、`temporaryCopies=True`、`gameWrites=False`、`ready=True`。
- 主预览、替换裁剪器、卡框预览、GUI 导出、文件拖出、命令行 `--export-card`、写入流程和统一超框编辑器共用 `GameTextureDisplayMapping`，预览所见的完整画布与写入前自动变形使用同一规则。
- 卡框透明插图区改用中心透明连通区域识别，外围阴影不再与中心插图区合并；64 套卡框全部具有独立插图区，灵摆效果框推荐与实际构图均为 `card_frame14`。

- 普通卡图预览回归：512×1024 Texture2D 输出仍为 512×1024，中心像素保持原图，未合成任何卡框；卡框增强失败时也回退显示原图而不是空白。
- 随包卡框明确分为 16 套普通、16 套透明 RGB、16 套 OfGradient 炫彩边缘和 16 套运行时派生透明炫彩框，共 64 套逻辑资源；分类固定按“透明卡框 → 透明炫彩卡框 → 炫彩卡框 → 普通卡框”显示。卡片目录推荐回归确认链接怪兽 22747 使用 `card_frame18`、灵摆效果怪兽 20486 使用 `card_frame14`、通常怪兽使用 `card_frame00`。
- 四类卡框除名称外还进行像素级防混淆断言：普通效果框有 442,667 个可见像素；Astellar 透明效果框有 296,016 个可见像素并保留 424,880 个透明 RGB 像素；透明炫彩效果框同样只有 296,016 个可见像素、保留 372,175 个透明 RGB 像素，并与透明框存在 368,340 个颜色差异、与普通炫彩框存在 258,525 个 Alpha 差异；Floowan 炫彩框与普通框存在 93,632 个差异像素。后续若把目录、Alpha 或标签接反，测试会直接失败。
- Astellar 透明边缘模式 16/16 模板通过：每套均准确载入 6 个图层，重新读取 PNG 后仍保留 Alpha=0 像素的 RGB。重新提取后的 `card_frame18` Dirty Mask 为 140,051 像素，0 个 Alpha 清除失败、0 个预览可见性失败，并保留 139,244 个非黑透明 RGB 像素。
- 从真实游戏卡号 3899 的 512×512 Texture2D 直接进入统一“制作超框”编辑器回归通过：首次打开默认透明超框，透明／透明炫彩／炫彩／普通四种模式各显示 16 套并生成 704×1024 输出；直接画布模式可用，测试使用临时素材目录且 `gameWrites=False`。
- 完整单编辑器超框工作流回归通过：普通卡与已有超框卡都直接进入 `OverFrameFrameEditorForm`，不存在旧裁剪中间页；同一窗口包含“更换卡图／添加叠底背景／清除背景”，卡图可直接拖动及缩放。切换四类卡框时位置不跳变，关闭并重新打开后缩放与 X／Y 偏移均恢复。
- 2.0.4 保持默认进入“真实 Alpha 预览”，并提供“构图编辑”切换；最终模式下拖动、滚轮、滑杆与方向键编辑均停用。编辑器预览 PNG 与输出 PNG 逐像素比较为 0 mismatch，透明像素超过 10,000；主界面预览控件确认为 `AlphaPreviewBox`。透明区域在两处都显示棋盘格，显式 RGB 诊断投影不再参与默认预览。
- 图层像素断言确认最终顺序为“叠底背景 → 卡框 → 透明主体”：主体 RGB 可覆盖框线，但 Astellar Dirty Mask 最终会对完整透明边缘清零 Alpha，包括主体与框线的交叠区域；ArtFrame／EffFrame 会排除 EffBox。透明卡框画布直接显示输出的真实 Alpha，游戏输出完整保留透明 RGB。真实 22747 草稿只读回归得到 140,051 个透明像素、139,993 个非黑透明 RGB 像素，`gameWrites=False`。
- 22747 临时 Bundle 写回往返：只复制目标 Bundle 到系统临时目录，写入 704×1024 合成结果后重新打开解码，全部 RGBA 像素差异为 0，139,993 个零 Alpha 隐藏 RGB 全部保留；正式游戏 Bundle 前后 SHA-256 一致。
- 2.0 实机只读验收：超框分类显示独立上下文条，Mod 管理显示导入／导出／备份动作；选中 22747 后信息区明确显示“原始 Texture2D（未叠加卡框）”，卡框编辑器为 `card_frame18 · 连接怪兽`，未执行任何应用或写回操作。

- 卡号 13587、14209、21959 在故意破坏 PathID、serialized file 名称和 Bundle 路径后，均成功重新定位及解码。
- 卡号 10866 的陈旧 PathID 和不存在的 Bundle 路径可跨 `LocalData`／`StreamingAssets` 自动重定位；恢复 PathID `2264973147763192676`，PNG 解码 430,853 字节。
- 三张卡均在临时 Bundle 副本中成功写回为 704×1024，重新打开后可再次解码。
- 视觉资源页实机显示全部一级筛选及数量：决斗场地 385、壁纸／大厅 307、卡背／卡套 269、卡盒 551、硬币 76、头像装饰 1,359；可直接进入对应资源列表。
- 视觉资源临时副本写回通过：
  - `ShopBGBase02`：大厅背景，1920×1080，PathID 1175。
  - `Mat_002_05_BaseColor_near`：决斗场地，1024×1024，PathID 809。
  - `WallPaper0001_1`：大厅壁纸，2048×1304，PathID -5488221142676854608。
- 原始动画资源六件套导出／导入回归：2 PNG、2 atlas、2 skeleton JSON，共 6 项全部在临时镜像中往返成功，并以 SHA-256 确认正式 LocalData 未改变。

## 怪兽动画

- 动画索引：495 张已安装动画卡、2,881 项资源、469 套完整六件套。
- Spine 4.2 全量只读兼容扫描：469/469 通过，0 failure、0 diagnostic、0 blank frame。
- 覆盖 weighted deform、path、physics、linkedmesh、clipping、IK/path/transform constraints、drawOrder、双色 tint、PMA、新旧 atlas 及非整数纹理缩放。
- 原版卡 15731：首帧优先显示，后台生成剩余 39 帧；初始 scale 100、实际资源 scale 35，取消式异步预览通过。
- 卡 10270 以“空输入框→输入纯数字卡号→380 ms 防抖自动定位”方式回归；在 39 帧渲染完成前即显示首帧并开始播放，`autoLocated=True`。重新输出开头、中段、末段三张采样，drawOrder、PMA 和时间线淡出顺序符合资源数据。
- HD／SD 资源配对、原始资源预览和六件套识别通过；卡 22747 同时找到 HighEnd_HD 与 SD 配对。
- Cpp2IL 与实机行为复核确认：`IsMonsterCutin` 只控制是否尝试加载，真正播放还依赖官方时间轴和内部登记；对没有官方演出的卡仅创建六个 Bundle 会在召唤时卡顿后无演出。
- 正式界面已移除无官方动画新增与运行时注入入口；动画服务不再包含模板选择和六 Bundle 创建代码。卡号 3899 是“闪刀姬－篝”的异画／公开编号，本身只有 2 项 skeleton；工具保留 `LocatedCardId=3899`，只读回退并播放同名完整官方演出 `P13668`，不会再误报为无动画，也不会把 P13668 误写成 3899 的独立六件套。真正没有官方演出的怪兽仍显示不支持新增。
- 3899 原始资源窗口回归：显示 P13668 的 HD／SD 共 6 项并可预览、导出；导入、单项替换、恢复与拖放写入全部禁用。连续执行 `3899 → 13668 → 3899` 三次加载均保持首帧、最终 `LocatedCardId=3899`／`PreviewSourceCardId=13668`，旧任务没有覆盖新选择，底部状态区无空字符或圆角 Region 残影。
- 兼容旧测试版：检测到历史 `Created` 动画事务时只允许预览及一键还原；还原会删除事务记录中的实验文件，不会继续创建或迁移资源。
- 随包动画索引 SHA-256：`D48B3376D714CD13170CC10D4BE1E07AE924588F3ECFF641C359372FA588A408`。

## 安全边界

最终验收时发现旧版“原始动画资源往返”测试曾短暂写入卡 15731 的 6 个正式 LocalData Bundle。已立即从该次写入前自动生成的首次备份恢复，并逐项确认 live 文件与备份 SHA-256 完全一致；这 6 份恢复依据另存于系统临时目录。随后已将该测试修正为只对临时 LocalData 镜像执行，并加入 live 文件前后哈希不变断言。

除上述已恢复且有哈希校验的测试问题外，其余写回回归均使用系统临时目录中的 Bundle／LocalData 镜像。当前版本不包含注入功能；没有修改游戏可执行文件、`GameAssembly.dll`、原 `data24`、Astellar 项目或任何旧发布目录。
