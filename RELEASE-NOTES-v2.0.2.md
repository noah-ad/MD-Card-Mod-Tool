# MD Card Mod Tool 2.0.2

## 透明预览与卡框分类

- 透明卡框与透明超框默认使用真实 Alpha 预览。Alpha=0 的区域显示棋盘格，不再被“游戏 RGB 投影”强制画成不透明颜色；编辑器预览、导出的最终 PNG 与实际写入 Texture2D 的 RGBA 保持一致。
- 透明像素中的 RGB 数据仍会在 PNG 编码和 Bundle 往返中原样保留。可见 RGB 投影仅保留为显式诊断能力，不参与默认预览或写回。
- “炫酷卡框”统一更名为“炫彩卡框”。卡框资源和制作超框编辑器现在固定显示四类：透明卡框、透明炫彩卡框、炫彩卡框、普通卡框，每类 16 套。
- 新增的透明炫彩卡框使用 Floowan OfGradient 的 RGB、Astellar 透明框的 Alpha，并在最终合成时套用相同 Dirty Alpha 几何；它与普通炫彩框具有可验证的颜色及 Alpha 差异。
- 主界面卡图预览改用暗色棋盘格控件；制作超框画布继续使用高对比棋盘格，透明区域在两处都能直接辨认。

## 兼容与安全

- 保留 2.0.1 的统一超框编辑器、卡图拖动／缩放、叠底背景、四语检索、视觉资源及官方怪兽动画预览。
- 所有游戏写回仍要求 Master Duel 完全退出并创建首次备份。开发回归只使用临时 Bundle 副本，不修改真实游戏文件。
