# 2.0.7 本地测试版

- 主窗口完成控件创建后统一执行首次 DPI 缩放，避免 150% 下侧栏仍使用 100% 尺寸；圆角按钮离屏表面匹配控件 DPI，导航图标按实际高度居中。
- 动画替换在构建前检查文件占用，所有资源在临时副本构建并验证后再提交。提交期间持有允许原子替换的文件句柄，防止新的普通扫描／预览读句柄插入。
- 使用 File.Replace 提交；失败仅还原已提交文件，回滚失败保留恢复副本。错误标题不再不加区分地声称“已回滚”。
- BC7 使用编码器默认搜索，不再强制高耗时的穷举模式；压缩格式仍为 BC7。

22524 临时六 Bundle 镜像已验证：占用提前失败、正常替换、提交中断回滚和备份还原，未写入真实游戏。耗时随帧数、分辨率、硬件变化。
100%、150%、200% 使用 WinForms 布局缩放路径回归；当前测试机实际 DPI 为 96，尚未完成真实 150% 桌面与跨屏 DPI 切换验收。
