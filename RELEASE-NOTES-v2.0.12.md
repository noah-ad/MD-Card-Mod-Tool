# 2.0.12 — 动画别名、多页与非数字目录识别修复

- 扩展到 14581（_noait_）、18825（ForUnity）等非卡号资产名，并恢复 3899 自身动画的定位与替换。
- 本机只读检查覆盖 507 张已检测骨骼的卡，发现 22 张不同名图集、341 张多页图集；修复按资源规则处理，不维护卡号特例表。识别结果不代表所有动画的游戏内效果均已验收。
- 从已找到的容器目录验证同名 HD/SD 配套资源，补齐 22341 的 img 目录；不再只猜测数字比例路径。
- 动画页每次定位均补查官方依赖，即使已有首页也不漏掉后续页；索引重建及缺失资源补全同步使用该规则。
- 多页预览改用分格布局，合成预览限制在 8192 每边以内，减少超长图集的尺寸与内存风险。游戏写入分辨率不受此限制影响。

- 对缺失的动画资源沿官方 Prefab 的依赖关系定位，并用容器所属卡号识别归属。6969 的官方骨骼为 P6969JS，而纹理／图集为 P9696，现支持此类别名，不硬编码卡号映射。
- 支持原版多页 Spine 图集预览，各页分别解码并映射区域坐标。写入新动画保留目标首页面的原纹理名与材质引用，原第二页文件不改动。
- 资源缺失时不再宣称“没有官方召唤演出”，改为提示资源未定位或不完整。
- 可以向 6969 导入视频／帧序列或替换为支持的单页来源动画。多页原版动画作为整套移植来源暂不支持，明确拦截，避免只复制第一页。
- 默认图集仍为 4096，可选 8192。真实游戏召唤效果未验收；开发验证只使用临时副本。
- 限制：6969 原版预览中的部分特效仍呈粉色块，复杂材质尚未与游戏一致。此修复解决资源漏匹配及替换资格，不宣称原版渲染完全还原。
