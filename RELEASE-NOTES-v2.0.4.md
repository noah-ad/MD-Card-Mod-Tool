# MD Card Mod Tool 2.0.4

## 卡套与灵摆卡图比例修复

- 卡套在编辑、预览和导出时使用正常卡牌比例 `704×1024`；写入前自动转换为 Master Duel 的 `512×1024` Texture2D 存储画布。
- 灵摆卡图在编辑、预览和导出时使用正常 `512×683` 比例；写入前把完整画布拉伸为游戏的 `512×1024` 存储纹理。
- 两类资源都使用完整画布双向映射，不裁顶部、不保留旧版 596 像素显示区，也不在底部补透明尾部。游戏完成自身 UV 映射后，外观与工具中的正常比例预览一致。
- 壁纸、大厅背景等尺寸恰好同为 `512×1024` 的其他 Texture2D 保持原生尺寸，不会误套卡套或灵摆映射。

## 全流程一致性

- 主资源预览、替换裁剪器、卡框预览、GUI 导出、拖出文件和命令行 `--export-card` 共用同一映射规则。
- 写入仍经过临时 Bundle 验证；正式游戏文件只有在用户确认替换时才会修改。
- 已经制作成 `704×1024` 超框的灵摆卡，其旧 `512×1024` 草稿源图也能按卡片／卡框元数据识别，并在重新打开编辑器时迁移为 `512×683`。
- 修正卡框透明插图区识别：外围透明阴影不会再和中心插图区合并，灵摆框与普通框保持各自真实构图窗口。
- 裁剪窗口启用 Per-Monitor DPI 缩放与稳定重绘；映射说明会同时标出“正常预览”和“游戏存储”尺寸。

## 验证

- 合成双向映射、旧草稿迁移、64 套卡框像素分类、主窗口／搜索／Alpha 预览和统一超框工作流均通过自动回归。
- 在本机真实卡套与灵摆 Bundle 的临时副本中完成写入、重新打开和几何往返，正式游戏 Bundle 前后 SHA-256 不变。
- 多步骤动画页和超框页交互后再强制重绘，差异分别为 2 与 0 个像素，低于 64 像素阈值。
- 发布包根目录仍只有 `MD卡图查看替换器.exe` 与 `data` 文件夹。
